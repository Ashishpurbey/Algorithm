#include<bits/stdc++.h>
using namespace std;
int main(){
    int items;
    cin>>items;
    vector<int>size(items);
    vector<int>value(items);
    for(int i=0;i<items;i++)cin>>size[i];
    for(int i=0;i<items;i++)cin>>value[i];
    
}